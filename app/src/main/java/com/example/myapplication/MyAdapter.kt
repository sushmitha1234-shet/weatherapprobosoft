package com.example.myapplication

import android.content.Context
import android.view.LayoutInflater
import android.view.ViewGroup
import androidx.recyclerview.widget.RecyclerView

//class MyAdapter(private val picture: IntArray, private val name: Array<String>, private val info:Array<String>, private val mContext:Context ):RecyclerView.Adapter<MyHolder>() {
   // override fun onCreateViewHolder(parent: ViewGroup, viewType: Int): MyHolder {
     //val v =LayoutInflater.from(parent?.context).inflate(R.layout.cardview,parent,false)
       // return MyHolder(v,mContext)
    //}

   // override fun getItemCount(): Int {
    //     return picture.size
    //}

   // override fun onBindViewHolder(holder: MyHolder, position: Int) {
      //  holder?.index(picture[position], name[position], info[position])
    //}


//}
